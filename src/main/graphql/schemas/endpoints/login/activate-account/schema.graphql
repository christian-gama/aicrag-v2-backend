type Mutation {
  activateAccount(input: ActivateAccountInput!): ActivateAccount @user @partialProtected
}

input ActivateAccountInput {
  email: EmailAddress!
  activationPin: String!
}

type ActivateAccount {
  accessToken: JWT!
  refreshToken: JWT!
  user: PublicUser!
}
