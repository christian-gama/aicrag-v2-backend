type Mutation {
  updatePassword(input: UpdatePasswordInput!): UpdatePassword! @protected
}

input UpdatePasswordInput {
  currentPassword: String!
  password: String!
  passwordConfirmation: String!
}

type UpdatePassword {
  user: PublicUser!
}
