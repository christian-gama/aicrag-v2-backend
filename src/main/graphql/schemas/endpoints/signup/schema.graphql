type Mutation {
  signUp(input: SignUpInput!): SignUp!
}

type SignUp {
  user: PublicUser!
}

input SignUpInput {
  email: EmailAddress!
  name: String!
  password: String!
  passwordConfirmation: String!
}
